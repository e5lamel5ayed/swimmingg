import React from 'react';

const Loading = () => {
    return (
        <div className="loader">
            <img src="images/swimming.webp" alt="Loading..." />
        </div>
    );
};

export default Loading;
